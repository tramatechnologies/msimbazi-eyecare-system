// Tailwind CSS is now handled by @tailwindcss/vite plugin in vite.config.ts
// This file is kept for backward compatibility or future PostCSS plugins
// autoprefixer is now handled internally by Tailwind CSS v4

export default {
  plugins: [
    // Tailwind and autoprefixer are handled by the Vite plugin
  ],
}
